doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    title Soundsnaps

  body 
    h1 Soundsnaps

    ul
      li
        a(href="/rss.xml") RSS
      li
        a(href="https://itunes.apple.com/us/podcast/soundsnaps/id1363831763") iTunes

    h2 Episodes
      each episode in episodes
        h3
          a(href=`/episodes/${episode.key}`)= episode.title
        ul
          li= new Date(episode.date).toDateString()
          li= episode.duration
